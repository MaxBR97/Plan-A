package DTO.Records.Model.ModelData;

/**
 *
 * @param name The parameter's name in the zimpl code
 * @param type The parameter's type
 */
public record ParameterDefinitionDTO(String name, DataTypes type) {
}
